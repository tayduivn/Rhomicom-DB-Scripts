/* Formatted on 12-20-2018 9:39:23 AM (QP5 v5.126.903.23003) */
CREATE OR REPLACE FUNCTION ORG.GET_DIV_TYPE_ID (P_DIVID NUMBER)
   RETURN NUMBER
AS
   L_RESULT   NUMBER;
BEGIN
   L_RESULT := -1;

   SELECT   DIV_TYP_ID
     INTO   L_RESULT
     FROM   ORG.ORG_DIVS_GROUPS
    WHERE   DIV_ID = P_DIVID;

   RETURN L_RESULT;
EXCEPTION
   WHEN OTHERS
   THEN
      RETURN -1;
END;
/